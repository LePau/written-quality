import { WrittenEnvironment } from './written-environment.model';

describe('WrittenEnvironment', () => {
  it('should create an instance', () => {
    expect(new WrittenEnvironment()).toBeTruthy();
  });
});
